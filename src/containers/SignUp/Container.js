import styled from 'styled-components';
import { Constants } from 'expo';

import Container from '../../components/Container';

const ContainerTop = Container.extend`
  justify-content: top;
`;

export default ContainerTop;
